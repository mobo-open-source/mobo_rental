import 'package:flutter/material.dart';

class ScanProducts extends StatelessWidget {
  const ScanProducts({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('scan Products')));
  }
}
